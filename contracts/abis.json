{
  "MerkleDistributor": [
    "function addDistribution(address _token, bytes32 _merkleRoot, uint256 _totalAmount, uint256 expiryTimestamp)",
    "function updateDistribution(address _token, bytes32 _merkleRoot, uint256 _additionalAmount, uint256 expiryTimestamp)",
    "function distributions(address _token) view returns (tuple(address token, bool isPaused, uint256 expiryTimestamp, bytes32 merkleRoot, uint256 totalAmount))"
  ],
  "ERC20": [
    "function approve(address _spender, uint256 _amount)",
    "function balanceOf(address _account) view returns (uint256)",
    "function transfer(address _receiver, uint256 _amount)"
  ]
}
